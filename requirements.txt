# 경량 필수 의존성 — DESIGN.md 5.1
# 주의: 기본 인페인팅은 LaMa(requirements-ml.txt)이며 torch 생태계가 numpy<2 를
#       요구하므로 여기서도 numpy<2 로 고정한다. LaMa 미설치 시 Telea 로 폴백.
numpy>=1.26,<2
opencv-python>=4.9,<5
Pillow>=9.5
pydantic>=2.6
matplotlib>=3.8
PySide6>=6.6
